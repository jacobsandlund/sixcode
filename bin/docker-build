#!/bin/bash -e

if [[ $1 == 'help' ]]; then
    echo 'Usage: st docker-build                Build docker image'
    exit 0
fi

docker build -f $SPACETIME_PATH/development/Dockerfile -t jacobsandlund/spacetime $SPACETIME_PATH
