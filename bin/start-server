#!/bin/bash -e

if ! grep -q 'docker\|lxc' /proc/1/cgroup 2>/dev/null; then
    docker exec -it sixcode bin/start-server "$@"
else
    nginx -c /sixcode/development/nginx.conf
    node development/server.js
fi
