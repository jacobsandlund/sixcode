#!/bin/bash -e

if [[ $1 == 'help' ]]; then
    echo 'Usage: st docker-exec [command]       Execute command (or bash) in docker'
    exit 0
fi

if [ "$#" -eq 0 ]; then
    command=/bin/bash
else
    command=$1
    shift
fi

docker exec -it spacetime $command "$@"
