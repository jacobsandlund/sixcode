#!/bin/bash -e

if ! grep -q 'docker\|lxc' /proc/1/cgroup 2>/dev/null; then
    docker exec -it sixcode bin/make "$@"
else
    /emscripten/emscripten/emmake make -f development/Makefile "-j$(nproc --all)" --no-print-directory "$@"
fi
