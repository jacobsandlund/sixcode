#!/bin/bash -e

if ! grep -q 'docker\|lxc' /proc/1/cgroup 2>/dev/null; then
    docker exec -it spacetime bin/make "$@"
else
    /deps/emscripten/emmake make -f development/Makefile "-j$(nproc --all)" --no-print-directory "$@"
fi
